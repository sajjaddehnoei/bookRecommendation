---
title: "code"
author: "Sajjad"
date: "3/19/2022"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r libraries}
library(tidyverse)
library(cluster)
```

```{r readData}
bookRatings <- read.csv("data/BX-Book-Ratings.csv", header = T, sep = ";")
books <- read.csv("data/BX-Books.csv", header = T, sep = ";")
users <- read.csv("data/BX-Users.csv", header = T, sep = ";")
```

```{r fakeCategories}
set.seed(1234)
categories = c("Action and Adventure","Classic","Detective and Mystery",
               "Fantasy")
books$category = sample(categories, nrow(books), replace=TRUE,
                        prob=c(0.25, 0.3, 0.25, 0.20))
books$category = as.factor(books$category)
```

